update_fastlane
fastlane_version "1.106.2"
default_platform :ios

platform :ios do
  desc "Runs all the tests"
  lane :test do
    scan(device: "#{"iPad Pro (9.7 inch)"}")
  end

  desc "Submit a new Beta Build to Apple TestFlight"
  lane :beta do
    increment_build_number
    gym
    pilot
  end
end
